import React from "react";

const ApplyNow = ({ open, action }) => {
  return <>
    {/* <div className="soststorm-appynow-wrapper">
      <div className={`soststorm-appynow-canvas ${open ? 'open' : ''}`}>
        <div className="soststorm-appynow">
          <div className="soststorm-appynow-top-bar d-flex justify-content-between">
            <h6>apply Now </h6>
            <button type="button" onClick={action} className="soststorm-appynow-close">
              <i className="fas fa-times"></i>
            </button>
          </div>
        </div>
      </div>
      <div className={`overlay ${open ? 'open' : ''}`}></div>
    </div> */}
  </>;
};

export default ApplyNow;
